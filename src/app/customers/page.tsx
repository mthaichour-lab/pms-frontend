import { getServerSession } from 'next-auth'; import Link from 'next/link'; import { redirect } from 'next/navigation'; import { authOptions } from '@/auth/options'; import { CustomerConsole } from './customer-console'; import styles from '../products/products.module.css'; export const metadata={title:'Clients | PMS'}; export const dynamic='force-dynamic'; export default async function CustomersPage(){if(!(await getServerSession(authOptions)))redirect('/api/auth/signin/oidc?callbackUrl=/customers');return <div className={styles.page}><header className={styles.header}><Link href="/">Retour au tableau de bord</Link><h1>Clients et restrictions</h1><p>Gérez les profils tokenisés et les blocages légaux sans exposer de données personnelles en clair.</p></header><main className={styles.main}><CustomerConsole/></main></div>}
