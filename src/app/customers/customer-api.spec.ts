import{afterEach,describe,expect,it,vi}from'vitest';import{createCustomer,liftCustomerRestriction}from'./customer-api';afterEach(()=>vi.restoreAllMocks());describe('customer API',()=>{it('uses an idempotency key',async()=>{const mock=vi.spyOn(globalThis,'fetch').mockResolvedValue(new Response('{}'));await createCustomer({customerId:crypto.randomUUID(),identityToken:'tok_1234567890123456',beneficialOwnerTokens:[],representativeTokens:[],segment:'RETAIL',kycStatus:'PENDING',legalForm:'PERSON',sectorCode:'RETAIL',branchCode:'001',restrictions:[]});expect(new Headers(mock.mock.calls[0]![1]?.headers).get('idempotency-key')).toMatch(/^[0-9a-f-]{36}$/)});it('lifts the selected restriction with a dated command',async()=>{const mock=vi.spyOn(globalThis,'fetch').mockResolvedValue(new Response('{}'));await liftCustomerRestriction('customer/id','restriction/id','2026-09-08');expect(mock.mock.calls[0]?.[0]).toContain('/customer%2Fid/restrictions/restriction%2Fid/lift');expect(mock.mock.calls[0]?.[1]?.body).toBe('{"liftedAt":"2026-09-08"}')})});
