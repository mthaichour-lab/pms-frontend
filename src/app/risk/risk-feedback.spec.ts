import {createElement}from'react';import{renderToStaticMarkup}from'react-dom/server';import{describe,expect,it}from'vitest';import{RiskError,RiskStatus}from'./risk-feedback';
describe('risk feedback',()=>{it('renders an assertive error',()=>{const html=renderToStaticMarkup(createElement(RiskError,{message:'Erreur corrélée'}));expect(html).toContain('role="alert"');expect(html).toContain('aria-live="assertive"')});it('renders a polite success',()=>{const html=renderToStaticMarkup(createElement(RiskStatus,{message:'Calcul terminé'}));expect(html).toContain('role="status"');expect(html).toContain('aria-live="polite"')})});
