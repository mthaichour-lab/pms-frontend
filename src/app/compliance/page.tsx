import { getServerSession } from 'next-auth'; import Link from 'next/link'; import { redirect } from 'next/navigation'; import { authOptions } from '@/auth/options'; import { ComplianceConsole } from './compliance-console'; import styles from '../products/products.module.css'; export const metadata = { title: 'Gouvernance de conformité | PMS' }; export const dynamic = 'force-dynamic'; export default async function CompliancePage() { if (!(await getServerSession(authOptions))) redirect('/api/auth/signin/oidc?callbackUrl=/compliance'); return <div className={styles.page}><header className={styles.header}><Link href="/">Retour au tableau de bord</Link><h1>Gouvernance de conformité</h1><p>Versionnez les références, consultez les règles effectives et arbitrez les divergences documentaires.</p></header><main className={styles.main}><ComplianceConsole /></main></div>; }
