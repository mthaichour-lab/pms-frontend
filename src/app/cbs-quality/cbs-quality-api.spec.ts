import {describe,expect,it} from 'vitest';import {qualitySummary} from './cbs-quality-api';describe('qualitySummary',()=>{it('aggregates operational quality indicators',()=>{expect(qualitySummary([{batchId:'1',sourceCode:'CBS',businessDate:'2026-08-29',flowType:'POSITIONS',sequenceNumber:1,state:'REJECTED',errorCount:3,warningCount:1}])).toEqual({batches:1,rejected:1,errors:3,warnings:1})})});
