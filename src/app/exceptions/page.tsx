import { getServerSession } from 'next-auth'; import Link from 'next/link'; import { redirect } from 'next/navigation'; import { authOptions } from '@/auth/options'; import { ExceptionConsole } from './exception-console'; import styles from '../products/products.module.css'; export const metadata = { title: 'Gestion des exceptions | PMS' }; export const dynamic = 'force-dynamic'; export default async function ExceptionsPage() { if (!(await getServerSession(authOptions))) redirect('/api/auth/signin/oidc?callbackUrl=/exceptions'); return <div className={styles.page}><header className={styles.header}><Link href="/">Retour au tableau de bord</Link><h1>Gestion des exceptions</h1><p>Détectez, qualifiez et suivez les écarts Finance, Risque et Charia jusqu’à leur clôture contrôlée.</p></header><main className={styles.main}><ExceptionConsole /></main></div>; }
