import { getServerSession } from 'next-auth'; import Link from 'next/link'; import { redirect } from 'next/navigation'; import { authOptions } from '@/auth/options'; import { OpeningBalanceConsole } from './opening-balance-console'; import styles from '../products/products.module.css';
export const metadata = { title: "Certification des soldes d'ouverture | PMS" }; export const dynamic = 'force-dynamic';
export default async function OpeningBalancesPage() { if (!(await getServerSession(authOptions))) redirect('/api/auth/signin/oidc?callbackUrl=/opening-balances'); return <div className={styles.page}><header className={styles.header}><Link href="/">Retour au tableau de bord</Link><h1>Soldes d’ouverture</h1><p>Rapprochez les quatre composantes historiques et certifiez leur reprise avec les preuves Finance.</p></header><main className={styles.main}><OpeningBalanceConsole /></main></div>; }
