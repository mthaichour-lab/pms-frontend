import { getServerSession } from 'next-auth'; import Link from 'next/link'; import { redirect } from 'next/navigation'; import { authOptions } from '@/auth/options'; import { TokenConsole } from './token-console'; import styles from '../products/products.module.css';
export const metadata = { title: 'Protection des données | PMS' }; export const dynamic = 'force-dynamic';
export default async function DataProtectionPage() { if (!(await getServerSession(authOptions))) redirect('/api/auth/signin/oidc?callbackUrl=/data-protection'); return <div className={styles.page}><header className={styles.header}><Link href="/">Retour au tableau de bord</Link><h1>Protection des données</h1><p>Tokenisez les identifiants personnels et exécutez les opérations privilégiées avec une finalité auditée.</p></header><main className={styles.main}><TokenConsole /></main></div>; }
